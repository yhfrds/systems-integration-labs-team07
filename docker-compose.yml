services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - db-data:/app/instance
    environment:
      - FLASK_APP=app.py
      - FLASK_DEBUG=0
      - SECRET_KEY=change-me-in-production
    restart: unless-stopped

volumes:
  db-data: # Persistent volume for the SQLite database